services:
  - type: web
    name: ares-backend
    env: node
    buildCommand: npm install --legacy-peer-deps
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: "20.10.0"
      - key: NODE_ENV
        value: production
